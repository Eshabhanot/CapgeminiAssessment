server:
  port: 6602

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Eshabhanot/Car-Microservices.git
          clone-on-start: true
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: "*"
